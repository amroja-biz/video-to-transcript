# yt-dlp[default] includes yt-dlp-ejs (bundled JS solver scripts for YouTube).
# A Deno runtime must be installed separately (brew install deno / Dockerfile handles it).
yt-dlp[default]>=2026.3.17
# PO-token provider client plugin; keep in sync with the bgutil server tag in Dockerfile.
bgutil-ytdlp-pot-provider>=1.3.1
boto3>=1.35
